{
  "name": "choir-seating-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace website dev",
    "build": "yarn workspace website build",
    "preview": "yarn workspace website preview",
    "cdk": "yarn workspace infrastructure cdk",
    "deploy": "yarn workspace website build && yarn workspace infrastructure cdk deploy",
    "synth": "yarn workspace infrastructure cdk synth",
    "website:lint": "yarn workspace website lint"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
